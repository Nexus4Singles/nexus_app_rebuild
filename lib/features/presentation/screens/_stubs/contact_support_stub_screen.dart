import 'package:flutter/material.dart';

class ContactSupportStubScreen extends StatelessWidget {
  const ContactSupportStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Contact Support (stub)')));
  }
}
