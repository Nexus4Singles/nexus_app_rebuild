import 'package:flutter/material.dart';

class SplashStubScreen extends StatelessWidget {
  const SplashStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Splash (stub)')));
  }
}
